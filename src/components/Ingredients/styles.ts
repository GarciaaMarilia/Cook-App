import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    height: 58,
    maxHeight: 58,
  },
  ingredientsContent: {
    gap: 12,
    paddingVertical: 14,
    marginHorizontal: 30,
  },
})